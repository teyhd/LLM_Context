Изменения
- train: использует готовый датасет из `data/output`, фиксированное имя прогона `vlad`, оценка/сохранение по эпохам и уведомление без эмодзи.
- bot: синхронизирован с форматом обучения — тот же системный промт и шаблон пользователя, сборка prompt через теги `[role]...[/role]`, на инференсе берётся вся пользовательская история (ограничение 40 сообщений) и укладывается в 2048 токенов.
- bot: путь адаптера по умолчанию `models/vlad/final_adapter`, контекст очищается командой `/clear`, имя для шаблона можно менять командой `/who`.
- bot: добавлено логирование в `bot.log` (вход, собранный prompt с токенами, ответ, тайминги).
